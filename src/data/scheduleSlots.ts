/**
 * Standard bookable time slots for vet appointments (Sep 12 2026 standup:
 * specific slots, e.g. 08:00–10:00, instead of blocking the whole day). Shared
 * by the booking form and the vet's calendar so both speak the same slot grid.
 */
export const BOOKING_SLOTS = ['08:00–10:00', '10:00–12:00', '12:00–14:00', '14:00–16:00', '16:00–18:00'];
